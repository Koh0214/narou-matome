.card.mx-3.my-2
  .card-title.h2.p-3.mb-0
    = @user.name
    さんのまとめ
  -if user_signed_in? && current_user.id == @user.id
    .card-body.px-3.pb-2.pt-0
      = link_to edit_user_registration_path(@user) do
        .fa.fa-pencil
          ユーザ情報編集

-# matomes#index と同じだからpartialに切り出しできるかも？
.container-fluid.m-0.p-0
  - @matomes.each do |matome|
    = link_to(matome_path(matome)) do
      .card.mx-auto.p-2{:style => "background-color: #F5F5F5;"}
        .card-title.mb-0
          .font-weight-bold{:style => "font-size:18px; color: black;"}
            = matome.title
            -# = matome.user.name
        -# %hr.m-1
        .card-body.p-0{style: "color: #666;" }
          = matome.description
          .container
            .row
              .small.text-muted.p-1.pt-2.pl-5{style: "font-size:11px"}
                %span{"class"=>"comment-count"}
                = matome.created_at.strftime("%Y/%m/%d/ %H:%M")
                【#{matome.impressionist_count} PV】
          -if user_signed_in? && current_user.id == matome.user_id
            = link_to edit_matome_path(matome) do
              .fa.fa-edit.btn.btn-primary.float-right{style: "font-size:15px"}
